@page "/"
@page "/{blob?}"

@* @if (blob is not null) *@
@* { *@
    <MarkBlobView Blob="@blob">
        <BlobLoading>
            <Loading/>
        </BlobLoading>
        <BlobNotFound>
            <NotFound/>
        </BlobNotFound>
    </MarkBlobView>
@* } *@

@code
{
    [Parameter] public string? blob { get; set; }
    
    protected override void OnParametersSet()
    {
        blob ??= "overview";
    }
}